name: CI/CD

on:
  push:
    branches:
      - master
  workflow_dispatch:
    
jobs:
  build_dev:
    uses: dongnguyenltqb/baseforci/.github/workflows/build.yml@master
    if: github.ref_name == 'master'
    with:
      environment: dev

